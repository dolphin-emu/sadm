# URLs to periodically fetch to get queue status.
url:
  - https://buildbot.dolphin-emu.org/json/builders/fifoci-dx-win-nv
  - https://buildbot.dolphin-emu.org/json/builders/pr-fifoci-dx-win-nv

# Number of consecutive tries that must return 0 pending before halting.
threshold: 10

# Interval between each fetch (in seconds).
interval: 10

shutdown_command: powershell shutdown.ps1
